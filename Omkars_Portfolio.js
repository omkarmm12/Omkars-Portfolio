var typed = new Typed("#dynamic-text", {
    strings: ["Developer", "Full Stack Developer", "Software Engineer"],
    typeSpeed: 60, // Speed at which each letter types out
    backSpeed: 20, // Speed at which it erases
    backDelay: 1000, // Delay before typing the next word
    startDelay: 500, // Initial delay before it starts typing
    loop: true, // Make it loop through the words
});